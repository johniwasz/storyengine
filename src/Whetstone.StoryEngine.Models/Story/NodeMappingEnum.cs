namespace Whetstone.StoryEngine.Models.Story
{
    public enum NodeMappingType
    {
        SingleNodeMapping = 0,
        MultiNodeMapping = 1,
        ConditionalNodeMapping = 3,
        SlotNodeMapping = 4,
        SlotMap = 5
    }
}
